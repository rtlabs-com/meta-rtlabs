This is cmake-native from https://github.com/openembedded/openembedded-core/ using the zeus branch.

That recipe is MIT licensed.

The resulting cmake-related files end up in build/tmp/sysroots-components/x86_64/cmake-zeus-native
(and cmake itself in its subdirectory usr/bin).
